This repository contains the source code for controlling the exoskeleton

The source projects, organised into folders in the `src` directory, are built around a platformio environment.

For more information about what pio is and how it works goto : https://platformio.org/

To run a project (called an `environment` in pio terms) do `pio run -e yourenv` where `yourenv` is the target environment. 

